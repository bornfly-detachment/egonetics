## 发布到 Chronicle" 功能恢复 是左边的标注block，不是右边的session（标注block包含了session）

## http://localhost:3000/api/agents/agent-1772544240070-nxsg0/messages 点击 agent后会自动划到网页底部

## Task 详情页 (/tasks/:id) 详情页「创建于/更新于」下方增加块编辑器 应该复用 BlockEditor，而不是实现一个新的