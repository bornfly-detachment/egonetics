基本编程要求：
一 优化代码，去除冗余代码，同时做好注释工作。实现高内聚，低耦合
二 接口清晰一致，数据库方便扩容，基于需求和数据结构特性设计


1 目前后端逻辑有些混乱，Task.db  和 /tasks /tasks/:taskId  并没真正联通；请重写后端代码，按照前端的数据结构设计后端
2 目前后端服务和端口不统一，比如说关键都用到了PageManager的实现（Task、chronicle、theory）；但是其实后续逻辑不同，memory也相对独立。Task应该会有非常丰富的数据类型，频繁的CRUD，不断叠加；chronicle、theory的内容数据类型基本只有文本，主要是类似一本书一样的结构。目录和版本管理比较重要，需要加密，对任意历史条目的修改都会破坏后续所有条目的哈希值，使篡改行为立即可被检测，每条编年史条目都与前一条密码学链接，memory是标注提炼查看历史信息记录用的，把历史对话从jsonl入库而已，后续基于其开发标注页面
目前主要这3快不同数据结构，请针对性设计后端数据库存储，接口，代码干净简洁
3 重构完所有前后端代码，尤其是server的代码整理清楚，更新readme
4 测试前后端接口的连通性
